# dsh-token-usage-dashboard 的 bundle patch：作为 bundle 层被 dsh plugin add 安装时，
# 本文件把插件行插入 profile 的插件栈（host 半 = 包默认导出 lib/index.js；
# client 半 = 由 web app 的 dsh.client 标记扫描 lib/client.js）。
- insert:
    - id: token-stats
      name: dsh-token-usage-dashboard
